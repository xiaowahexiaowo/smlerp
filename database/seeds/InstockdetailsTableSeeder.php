<?php

use Illuminate\Database\Seeder;
use App\Models\Instockdetail;

class InstockdetailsTableSeeder extends Seeder
{
    public function run()
    {
        // $instockdetails = factory(Instockdetail::class)->times(50)->make()->each(function ($instockdetail, $index) {
        //     if ($index == 0) {
        //         // $instockdetail->field = 'value';
        //     }
        // });

        // Instockdetail::insert($instockdetails->toArray());
    }

}

